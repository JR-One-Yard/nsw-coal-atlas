# Historical browser scripts

These scripts target the version 2 interface, including controls deliberately removed in version 3. They are retained for reproducing earlier evidence with the corresponding Git revision. Do not use them to verify the current viewer. Use `npm run verify:browser` instead.

The recorded MP4 remains the existing version 2 presentation; it is a downloadable recording, not a recording of the current interface.
